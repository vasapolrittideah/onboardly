import { Meta, StoryObj } from '@storybook/react';

import * as {{properCase name}} from './{{kebabCase name}}';

const meta: Meta<typeof {{properCase name}}.Root> = {
  title: '{{properCase name}}',
  component: {{properCase name}}.Root,
};

export default meta;

type Story = StoryObj<typeof {{properCase name}}.Root>;

export const Default: Story = {
  render: () => (

  ),
};