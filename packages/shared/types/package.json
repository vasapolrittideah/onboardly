{
  "name": "@peeko/prisma-schema",
  "description": "Type declarations for Peeko project",
  "types": "types/index.d.ts",
  "version": "1.0.0",
  "scripts": {
    "generate": "bunx prisma generate",
    "postinstall": "tsc && bun run generate"
  },
  "devDependencies": {
    "@prisma/client": "^6.2.1",
    "@types/bun": "latest",
    "prisma": "^6.2.1",
    "prisma-types-export": "^1.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
